
@import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;500;700&display=swap');

*{
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}
:root{
    --main-one:rgb(133, 147, 156);
    --main-two:rgb(53, 120, 150);
    --main-tree: rgb(30, 76, 98);
    --main-four:rgba(21, 33, 43, 0.599);
}
body{
    font-family: 'Poppins', sans-serif;
    background-image: url(backgroundimage.jpg);
    background-repeat: no-repeat;
    background-size: 1440px 800px;
}

a {
    color: #fff;
    text-decoration: none;
}

.container {
    width: 80%;
    margin: auto;

}
header{
    height: 100px;
    border-bottom: 2px solid var(--main-four);
}

header nav{
    display: flex;
    justify-content: space-between;
    align-items: center;
    height: 100%;
}

header nav .logo a{
    font-weight: 900;
    font-size: 45px;
}

nav ul{
    list-style: none;
    display: flex;
    column-gap: 30px;
}

nav ul li a:hover{
    color: var(--main-two);
    transition: color 0.25s ease;
}

nav .menu{
    display: flex;
    align-items: center;
    column-gap: 45px;
}

nav .menu .menu-button{
    color: var(--main-two);
    background-color: rgba(255, 255, 255, 0.08);
    padding:12px 24px ;
    border-radius: 50px;
}

nav .menu .menu-button:hover{
    background-color: var(--main-one);
    transition: all 0.25s ease;
    color: #fff;
}

.content{
    color: #fff;
    height: calc(100vh - 100px);
    padding-top: 120px;
}

.content h1{
    font-size: 50px;
    text-align: center;
}
.content p{
    font-size: 20px;
    text-align: center;
    margin-top: 30px;
}
.content .link{
    display: flex;
    justify-content: center;
    margin-top: 30px;
    column-gap: 15px;
}
.content .link a{
    background-color: #fff;
    padding: 15px;
    width: 230px;
    color: black;
    text-align: center;
    font-size: 18px;
    text-decoration: none;
    border-radius: 50px;
    transition: all 0.25s ease-in ;
}
.content .link a:hover{
    background-color: var(--main-two);
    color: #fff;
}

@media(max-width:576px){
    .container{
        width: 100%;
        padding-left: 12px;
        padding-right: 12px;
    }
    .menu .menu-button{
        display: none;
    }
    .content{
        padding-top: 50px;
    }
    .content h1{
        font-size: 30px;
    }
    .content p{
        font-size: 15px;
    }
    .content .link a{
        padding:14px;
        width: 90%;
    }
    .content .link{
        flex-direction: column;
        align-items: center;
        row-gap: 1rem;
    }
}

